#include <stdio.h>
#include <math.h>
 
int main()
{
    float  a = 5.654;
    float a1 = round(a);
    printf("Lam tron so %f ta duo: %f", a, a1);
     
    float  b = 5.454;
    float b1 = round(b);
    printf("\nLam tron so %f ta duo: %f", b, b1);
    return 0;
}